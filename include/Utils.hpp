#pragma once

#include "Bar.hpp"

Bar1min::Timestamp parseRFC3339UTCTimestamp(const std::string& timestamp);

// Bar createBarFromCSVLine(const std::string& line);
// std::vector<Bar> createBarsFromCSV(const std::string& csvPath);
